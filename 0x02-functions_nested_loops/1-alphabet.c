#include <stdio.h>

/**
 * main - Entry point 
 *
 * Description: prints the alphabet, in lowercase
 *
 * Return: always 0 (success)
 */

int print_alphabet(void)
{
	alph = a;
	putchar('a', alph)
		a++;

