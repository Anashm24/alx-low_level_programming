#include "main.h"
#include <ctype.h>

/**
 * _islower - entry point
 *
 * Description: check 'c' if it is lowercase or not
 *
 * Return: always 0
 */

int _islower(int c)
{
	int c;

	c = _islower('c');
		return (1);
	c = _islower('C');
		return (0);
}
